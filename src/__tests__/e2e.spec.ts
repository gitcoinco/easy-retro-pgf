describe("E2E Tests", () => {
  test("browse projects", async () => {
    //
    expect(true).toEqual(true);
  });
  test("browse lists", async () => {
    //
    expect(true).toEqual(true);
  });
  test("add project to ballot", async () => {
    //
    expect(true).toEqual(true);
  });
  test("add list to ballot", async () => {
    //
    expect(true).toEqual(true);
  });
  test("view ballot", async () => {
    //
    expect(true).toEqual(true);
  });
  test("edit ballot", async () => {
    //
    expect(true).toEqual(true);
  });
  test("publish ballot", async () => {
    //
    expect(true).toEqual(true);
  });
});
